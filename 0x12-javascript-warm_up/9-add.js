#!/usr/bin/node

const first = parseInt(process.argv[2]);
const second = parseInt(process.argv[3]);
/*
if (first === undefined || second === undefined || isNaN(first) === true || isNaN(second) === true) {
  console.log('NaN');
} */
console.log(first + second);
